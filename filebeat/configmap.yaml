apiVersion: v1
kind: ConfigMap
metadata:
  name: filebeat-config
  namespace: logging
data:
  filebeat.yml: |
    filebeat.inputs:
      - type: filestream
        id: kubernetes-containers
        paths:
          - /var/log/containers/*.log

        # REQUIRED for Kubernetes symlinks
        prospector:
          scanner:
            symlinks: true
            scan_frequency: 10s

        parsers:
          - container: ~

        multiline:
          pattern: '^(Exception|\s+at )'
          negate: false
          match: after

    processors:
      # Add Kubernetes metadata FIRST
      - add_kubernetes_metadata:
          host: ${NODE_NAME}
          matchers:
            - logs_path:
                logs_path: "/var/log/containers/"

      # Keep ONLY logs from namespace: lab
      - drop_event:
          when:
            not:
              equals:
                kubernetes.namespace: "lab"

      # Decode JSON logs (if message is JSON)
      - decode_json_fields:
          fields: ["message"]
          target: ""
          overwrite_keys: true
          add_error_key: true

    output.logstash:
      hosts: ["logstash.logging.svc.cluster.local:5044"]

    logging.level: debug
    logging.to_files: false
